<!-- res/drawable/button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background_color_btn" /> <!-- Cor de fundo -->
    <corners android:radius="8dp" /> <!-- Borda arredondada -->
    <stroke
        android:width="2dp"
        android:color="@color/background_color_btn_border" /> <!-- Cor da borda -->
</shape>
